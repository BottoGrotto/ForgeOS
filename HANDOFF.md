# ForgeOS Handoff

This tracked handoff is intentionally concise.

Use `README.md` as the durable source of truth for setup, environment variables, runtime behavior, provider configuration, routes, and verification commands.

Do not store verbose transient session state, one-off debugging notes, local run results, or short-lived next-step lists here. Keep that material outside tracked source unless it is stable enough to promote into `README.md` or another project document.
